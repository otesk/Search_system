spring:

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/doc?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: OTESKone2020
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
      jdbc.lob.non_contextual_creation: true
